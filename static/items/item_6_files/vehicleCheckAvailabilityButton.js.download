"use strict";
class VehicleCheckAvailabilityButton {
    constructor() {
        document.addEventListener("DOMContentLoaded", this.initialize);
    }
    initialize() {
        const targets = "[data-dlron-type='check_availability']";
        var targetElems = document.querySelectorAll(targets);
        targetElems.forEach((elem) => {
            elem.addEventListener("click", (e) => {
                this.setupCheckAvailabilityButton(e);
            });
        });
    }
    setupCheckAvailabilityButton(e) {
        const elem = e.currentTarget;
        TrackGeneralCTA(elem);
    }
}
DoUtility.SelfInstantiateOnLoad(VehicleCheckAvailabilityButton);
//# sourceMappingURL=vehicleCheckAvailabilityButton.js.map